const ro = parseInt(prompt('enter price ronaldo='));

// white space

const mes = parseInt(prompt('enter price messi=')); // ; is the end of statements


// white space 

const sum = ro + mes;

console.log(`Total psg buy ronaldo with ${ro} and messi ${mes} is ${sum} Euro `);

// {code in here} => block

if (i => 6) {
    console.log('hoc sinh kha');
}
else {
    console.log('hoc sinh trung binh')
}

// behind // is comment

/* is block comment */